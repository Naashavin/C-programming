#include<stdio.h>
#include<math.h>
int main()
{
	int a=7,square,cube;
	square=(pow(a,2));
	cube=(pow(a,3));
	printf("the square of given numbers is %d,\n the cube root of given number is %d",square,cube);
	return 0;
}
